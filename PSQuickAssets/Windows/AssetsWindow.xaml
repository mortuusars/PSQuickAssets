<mui:WindowBase
    x:Class="PSQuickAssets.Windows.AssetsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mui="clr-namespace:MortuusUI;assembly=MortuusUI"
    Title="AssetsWindow"
    Width="500"
    Height="300"
    d:BorderThickness="0"
    d:WindowStyle="None"
    Background="{StaticResource BackgroundFarBrush}"
    BorderThickness="1"
    TitleBackground="SeaGreen"
    WindowStyle="SingleBorderWindow"
    mc:Ignorable="d">

    <b:Interaction.Behaviors>
        <mui:WindowChromeRenderedBehavior />
    </b:Interaction.Behaviors>

    <mui:WindowBase.Resources>
        <!--
            Due to the bug in XAML code generation - to use event handlers
            we need to define TitleContent of the window as a separate resource
        -->
        <Grid x:Key="TitleContent" Margin="8" WindowChrome.IsHitTestVisibleInChrome="True">
            <Ellipse
                Width="20"
                Height="20"
                MouseDown="Ellipse_MouseDown">
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Fill" Value="LightBlue" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Fill" Value="DeepSkyBlue" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>
        </Grid>
    </mui:WindowBase.Resources>

    <mui:WindowBase.Style>
        <Style TargetType="mui:WindowBase">
            <Setter Property="BorderBrush" Value="{StaticResource WindowBorderBrush}" />
            <Setter Property="TitleContent" Value="{StaticResource TitleContent}" />
            <Style.Triggers>
                <Trigger Property="IsActive" Value="False">
                    <Setter Property="BorderBrush" Value="{StaticResource BackgroundFarBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </mui:WindowBase.Style>

    <Grid x:Name="RootBorder" Background="Transparent">
        <d:ContentPresenter Height="36" VerticalAlignment="Top" Content="{StaticResource TitleContent}"/>
    </Grid>
</mui:WindowBase>
